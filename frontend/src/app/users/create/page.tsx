import CreateUserPage from "@/components/layout/CreateUser";

export const metadata = {
  title: "Create User",
  description: "Create a new user.",
};

export default function CreateUser() {
  return <CreateUserPage />;
}
